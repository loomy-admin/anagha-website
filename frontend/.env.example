# Anagha Express BFF (not the Octis ERP port)
BACKEND_URL=http://localhost:4001

# Production (Vercel): use your Render service URL, not the Render service ID
# BACKEND_URL=https://anagha-backend-xxxx.onrender.com
