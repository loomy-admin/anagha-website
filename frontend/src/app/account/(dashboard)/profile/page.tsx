import AccountProfile from '@/components/AccountProfile';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'My Profile | Anagha',
};

export default function AccountProfilePage() {
  return <AccountProfile />;
}
